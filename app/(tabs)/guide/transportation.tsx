import { View, Text } from 'react-native';
import React from 'react';

const TransportationTabView = () => {
  return (
    <View>
      <Text>TransportationTabView</Text>
    </View>
  );
};

export default TransportationTabView;
